{
  "id": "npqs-ephyto-upload--polling-msg",
  "template": "### IPPC ePhyto Hub Delivery Status\n\nThe phytosanitary certificate has been transmitted to the IPPC ePhyto Hub. The delivery status is now checked **automatically every minute** — no action is needed. The certificate shows as `PendingDelivery` until the importing country's NPPO downloads it, then `Delivered`, which completes this step.\n\n- **Reference Number**: `{{ .reference_number }}`\n- **Certificate Number**: `{{ .certificate_id }}`\n{{ with .ephyto }}{{ if .tracking_number }}- **Hub Tracking Number**: `{{ .tracking_number }}`\n{{ end }}{{ end }}{{ with .ephyto_poll }}{{ if .tracking_info }}- **Current Status**: `{{ .tracking_info }}`\n{{ end }}{{ if .error }}\n> {{ .error }}\n{{ end }}{{ end }}\n_This page refreshes as each check completes._"
}
